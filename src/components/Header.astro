---
import { slide } from "astro:transitions";
const { pathname } = Astro.url;
---

<header>
    <nav class="pb-10 font-mono">
        <div class="flex w-full justify-between items-center">
            <div class="flex gap-4">
                <a
                    href="/"
                    class={`font-medium text-base text-neutral-800 underline-offset-4 ${pathname === "/" ? "underline" : ""}`}
                    >an</a
                >
                <a
                    href="/about"
                    class={`font-medium text-base text-neutral-800 underline-offset-4 ${pathname === "/about" ? "underline" : ""}`}
                >
                    about
                </a>
                <a
                    href="/til"
                    class={`font-medium text-base text-neutral-800 underline-offset-4 ${pathname === "/til" ? "underline" : ""}`}
                    >til</a
                >
                <a
                    href="/notes"
                    class={`font-medium text-base text-neutral-800 underline-offset-4 ${pathname === "/notes" ? "underline" : ""}`}
                    >notes</a
                >
                <a
                    href="/bookmarks"
                    class={`font-medium text-base text-neutral-800 underline-offset-4 ${pathname === "/bookmarks" ? "underline" : ""}`}
                    >bookmarks</a
                >
                <a
                    href="https://twitter.com/duc__an"
                    class={`font-medium text-base text-neutral-800 underline-offset-4`}
                    target="_blank"
                    rel="noopener noreferrer"
                >
                    twitter
                </a>
                <a
                    href="/rss.xml"
                    class={`font-medium text-base text-neutral-800 underline-offset-4`}
                    >rss</a
                >
            </div>
        </div>
    </nav>
</header>
