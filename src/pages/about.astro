---
import Default from "@/layouts/Default.astro";
import { getCollection } from "astro:content";
const posts = await getCollection("blog", (entry) => !entry.data.draft);
---

<Default>
  Hello! I'm Duc An, a software engineer based in Vietnam. I'm passionate about building
      high-quality software solutions and continuously learning new technologies.
</Default>
